import { defineConfig } from 'astro/config';

export default defineConfig({
  // Deploy to /dataviz-style-guide/ subpath on GitHub Pages,
  // or change base to '/' for Netlify/Vercel root deployment
  // base: '/dataviz-style-guide/',
});
